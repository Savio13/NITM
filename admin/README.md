# Admin Panel (React Admin)

This is a minimal React Admin skeleton. To run:

```powershell
cd admin
npm install
npm run dev
```

By default it points to `/api` as the REST backend. We'll later provide an OpenAPI-backed adapter or a custom dataProvider.
